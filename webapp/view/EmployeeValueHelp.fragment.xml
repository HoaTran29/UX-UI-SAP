<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core">
    
    <!-- Thay SelectDialog thành ResponsivePopover -->
    <ResponsivePopover
        id="empValueHelpDialog"
        title="Select Employee"
        placement="Bottom"
        contentWidth="350px"
        contentHeight="400px">
        
        <!-- Nút Đóng trên cùng bên phải -->
        <endButton>
            <Button text="Close" press=".onEmployeeValueHelpCancel" />
        </endButton>
        
        <!-- Thanh Tìm Kiếm -->
        <subHeader>
            <Toolbar>
                <SearchField placeholder="Search..." search=".onEmployeeValueHelpSearch" liveChange=".onEmployeeValueHelpSearch" width="100%" />
            </Toolbar>
        </subHeader>
        
        <!-- Danh sách Nhân Viên -->
        <content>
            <List
                id="empValueHelpList"
                items="{/Employee}"
                mode="SingleSelectMaster"
                selectionChange=".onEmployeeValueHelpConfirm"
                noDataText="No employees found">
                <StandardListItem
                    title="{Ename}"
                    description="{Pernr}"
                    info="{DeptId}"
                    type="Active" />
            </List>
        </content>
        
    </ResponsivePopover>
</core:FragmentDefinition>